import colors from './Colors'
 const Theme = {
  colors: {
    placeholder: colors.grey_clr,
    text: colors.black_clr,
    primary: colors.red_clr,
  
   // background: 'transparent',
  },
};

export default Theme